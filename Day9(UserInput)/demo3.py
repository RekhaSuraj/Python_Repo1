m = 10
n1 = eval(input('Enter a number'))
print(n1)
print(type(n1))

# Python eval() function parse the expression argument and evaluate
# it as a Python expression and runs Python expression (code) within the program..

# Write a program to take inputs from the user for 5 subjects and calculate percentage
# Average marks also needs to be evaluated